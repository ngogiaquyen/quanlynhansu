<UserControl x:Class="HRMANAGMENT2.Views.RecruitmentPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <WrapPanel Grid.Row="0" Margin="20" Background="#F8F9FA">
            <Label Content="Tìm kiếm:" VerticalAlignment="Center" Margin="5" FontWeight="Bold"/>
            <TextBox x:Name="TxtSearchRecruitments" Width="250" Margin="5" TextChanged="TxtSearchRecruitments_TextChanged"/>
            <Button Content="🔍 Tìm" Click="BtnSearchRecruitments_Click" Margin="5" Padding="12,8" Background="#007BFF" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="➕ Thêm" Click="BtnAddRecruitment_Click" Margin="5" Padding="12,8" Background="#28A745" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="✏️ Sửa" Click="BtnUpdateRecruitment_Click" Margin="5" Padding="12,8" Background="#FFC107" Foreground="Black" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="🗑️ Xóa" Click="BtnDeleteRecruitment_Click" Margin="5" Padding="12,8" Background="#DC3545" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="📊 Xuất Excel" Click="BtnExportRecruitments_Click" Margin="5" Padding="12,8" Background="#6F42C1" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </WrapPanel>

        <!-- DataGrid -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <DataGrid x:Name="DgvRecruitments" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" AlternatingRowBackground="LightGray" HeadersVisibility="Column" MouseDoubleClick="DgvRecruitments_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã TD" Binding="{Binding RecruitmentId}" Width="100"/>
                    <DataGridTextColumn Header="Mã NV" Binding="{Binding EmployeeId}" Width="100"/>
                    <DataGridTextColumn Header="Đơn ứng tuyển" Binding="{Binding JobApplicationPath}" Width="*"/>
                    <DataGridTextColumn Header="Hồ sơ" Binding="{Binding ResumePath}" Width="*"/>
                    <DataGridTextColumn Header="Bằng cấp" Binding="{Binding DegreesPath}" Width="*"/>
                    <DataGridTextColumn Header="Khám sức khỏe" Binding="{Binding HealthCheckPath}" Width="*"/>
                    <DataGridTextColumn Header="CV" Binding="{Binding CVPath}" Width="*"/>
                    <DataGridTextColumn Header="Thư giới thiệu" Binding="{Binding ReferenceLetterPath}" Width="*"/>
                    <DataGridTextColumn Header="Biên bản phỏng vấn" Binding="{Binding InterviewMinutesPath}" Width="*"/>
                    <DataGridTextColumn Header="Thư mời" Binding="{Binding OfferLetterPath}" Width="*"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>