<UserControl x:Class="HRMANAGMENT2.Views.DisciplinePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <Border Grid.Row="0" Margin="20" Background="#F8F9FA">
            <WrapPanel>
                <Label Content="Tìm kiếm:" VerticalAlignment="Center" Margin="5" FontWeight="Bold"/>
                <TextBox x:Name="TxtSearchDisciplines" Width="250" Margin="5" TextChanged="TxtSearchDisciplines_TextChanged"/>
                <Button Content="🔍 Tìm" Click="BtnSearchDisciplines_Click" Margin="5" Background="#007BFF" Foreground="White" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="➕ Thêm" Click="BtnAddDiscipline_Click" Margin="5" Background="#28A745" Foreground="White" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="✏️ Sửa" Click="BtnUpdateDiscipline_Click" Margin="5" Background="#FFC107" Foreground="Black" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="🗑️ Xóa" Click="BtnDeleteDiscipline_Click" Margin="5" Background="#DC3545" Foreground="White" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="📊 Xuất Excel" Click="BtnExportDisciplines_Click" Margin="5" Background="#6F42C1" Foreground="White" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </WrapPanel>
        </Border>

        <!-- DataGrid -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <DataGrid x:Name="DgvDisciplines" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" AlternatingRowBackground="LightGray" HeadersVisibility="Column" MouseDoubleClick="DgvDisciplines_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã KL" Binding="{Binding DisciplineId}" Width="100"/>
                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding EmployeeInfo}" Width="*"/>
                    <DataGridTextColumn Header="Vi phạm" Binding="{Binding ViolationPath}" Width="*"/>
                    <DataGridTextColumn Header="Quyết định KL" Binding="{Binding DisciplinaryDecisionPath}" Width="*"/>
                    <DataGridTextColumn Header="Thư từ chức" Binding="{Binding ResignationLetterPath}" Width="*"/>
                    <DataGridTextColumn Header="Chấm dứt" Binding="{Binding TerminationDecisionPath}" Width="*"/>
                    <DataGridTextColumn Header="Bàn giao" Binding="{Binding HandoverPath}" Width="*"/>
                    <DataGridTextColumn Header="Thanh lý" Binding="{Binding LiquidationPath}" Width="*"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>
