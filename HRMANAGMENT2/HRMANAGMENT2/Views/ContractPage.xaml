<UserControl x:Class="HRMANAGMENT2.Views.ContractPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <WrapPanel Grid.Row="0" Margin="20" Background="#F8F9FA">
            <Label Content="Tìm kiếm:" VerticalAlignment="Center" Margin="5" FontWeight="Bold"/>
            <TextBox x:Name="TxtSearchContracts" Width="250" Margin="5" TextChanged="TxtSearchContracts_TextChanged"/>
            <Button Content="🔍 Tìm" Click="BtnSearchContracts_Click" Margin="5" Padding="12,8" Background="#007BFF" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="➕ Thêm" Click="BtnAddContract_Click" Margin="5" Padding="12,8" Background="#28A745" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="✏️ Sửa" Click="BtnUpdateContract_Click" Margin="5" Padding="12,8" Background="#FFC107" Foreground="Black" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="🗑️ Xóa" Click="BtnDeleteContract_Click" Margin="5" Padding="12,8" Background="#DC3545" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="📊 Xuất Excel" Click="BtnExportContracts_Click" Margin="5" Padding="12,8" Background="#6F42C1" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </WrapPanel>

        <!-- DataGrid -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <DataGrid x:Name="DgvContracts" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" AlternatingRowBackground="LightGray" HeadersVisibility="Column" MouseDoubleClick="DgvContracts_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã HĐ" Binding="{Binding ContractId}" Width="100"/>
                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding EmployeeInfo}" Width="*"/>
                    <DataGridTextColumn Header="Ngày bắt đầu" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Ngày kết thúc" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Loại HĐ" Binding="{Binding ContractType}" Width="*"/>
                    <DataGridTextColumn Header="Phụ lục" Binding="{Binding ContractAnnexPath}" Width="*"/>
                    <DataGridTextColumn Header="Bảo mật" Binding="{Binding ConfidentialityAgreementPath}" Width="*"/>
                    <DataGridTextColumn Header="Không cạnh tranh" Binding="{Binding NonCompeteAgreementPath}" Width="*"/>
                    <DataGridTextColumn Header="Bổ nhiệm" Binding="{Binding AppointmentDecisionPath}" Width="*"/>
                    <DataGridTextColumn Header="Tăng lương" Binding="{Binding SalaryIncreaseDecisionPath}" Width="*"/>
                    <DataGridTextColumn Header="Thưởng" Binding="{Binding RewardDecisionPath}" Width="*"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>