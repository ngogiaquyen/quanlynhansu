<UserControl x:Class="HRMANAGMENT2.Views.TrainingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <WrapPanel Grid.Row="0" Margin="20" Background="#F8F9FA">
            <Label Content="Tìm kiếm:" VerticalAlignment="Center" Margin="5" FontWeight="Bold"/>
            <TextBox x:Name="TxtSearchTrainings" Width="250" Margin="5" TextChanged="TxtSearchTrainings_TextChanged"/>
            <Button Content="🔍 Tìm" Click="BtnSearchTrainings_Click" Margin="5" Padding="12,8" Background="#007BFF" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="➕ Thêm" Click="BtnAddTraining_Click" Margin="5" Padding="12,8" Background="#28A745" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="✏️ Sửa" Click="BtnUpdateTraining_Click" Margin="5" Padding="12,8" Background="#FFC107" Foreground="Black" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="🗑️ Xóa" Click="BtnDeleteTraining_Click" Margin="5" Padding="12,8" Background="#DC3545" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="📊 Xuất Excel" Click="BtnExportTrainings_Click" Margin="5" Padding="12,8" Background="#6F42C1" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </WrapPanel>

        <!-- DataGrid -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <DataGrid x:Name="DgvTrainings" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" AlternatingRowBackground="LightGray" HeadersVisibility="Column" MouseDoubleClick="DgvTrainings_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã ĐT" Binding="{Binding TrainingId}" Width="100"/>
                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding EmployeeInfo}" Width="*"/>
                    <DataGridTextColumn Header="Kế hoạch ĐT" Binding="{Binding TrainingPlanPath}" Width="*"/>
                    <DataGridTextColumn Header="Chứng chỉ" Binding="{Binding CertificatePath}" Width="*"/>
                    <DataGridTextColumn Header="Đánh giá" Binding="{Binding EvaluationPath}" Width="*"/>
                    <DataGridTextColumn Header="Lộ trình nghề" Binding="{Binding CareerPath}" Width="*"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>